This is a compiler (interpreter) built from scratch to understand how programming languages actually work under the hood.

It covers two ends of the stack: parsing source code into an abstract syntax tree (AST) — the same technique behind linters, static analyzers, and metaprogramming tools — and going low enough to deal with assembly and calling conventions, the gritty details of how a CPU actually executes a function call
